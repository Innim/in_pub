name: unpub_aws
description: A collection of modules to use for deploying in_pub into AWS infrastructure.
version: 0.1.0
homepage: https://github.com/bytedance/unpub
environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  in_pub: ^3.0.0
  minio: ^3.0.0
  http: ^0.13.3

dev_dependencies:
  lints: ^1.0.0
  test: ^1.17.12
  chunked_stream: ^1.4.1
  mockito: ^5.0.16
  build_runner: ^2.1.2
  path: ^1.8.0
  json_annotation: ^4.1.0
# dependency_overrides:
#   in_pub:
#     path: ../in_pub
