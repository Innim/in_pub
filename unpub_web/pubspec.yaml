name: unpub_web
description: A web app that uses AngularDart Components
version: 1.0.0
publish_to: none
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  ngdart: 8.0.0-dev.4
  ngforms: 5.0.0-dev.3
  ngrouter: 4.0.0-dev.3
  http: ^0.13.3
  # Used directly by app_service.dart; it resolved only through ngdart.
  intl: ^0.18.1
  markdown: ^4.0.0
  unpub_api:
    path: ../unpub/lib/unpub_api
dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.4.0
